<Window x:Class="DataCollector.Desktop.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="DataCollector Enterprise"
        Width="1540"
        Height="920"
        MinWidth="1280"
        MinHeight="820"
        FontFamily="Microsoft YaHei UI"
        Background="#EEF3F8">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Background="#132238" Padding="24,18">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock FontSize="26" FontWeight="Bold" Foreground="White" Text="DataCollector Enterprise" />
                    <TextBlock Margin="0,6,0,0" Foreground="#C9D7E8" Text="企业级 CNC 数采平台客户端 | 设备管理、日报统计、时间轴、用户角色" />
                </StackPanel>
                <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                    <TextBlock x:Name="ServerStatusTextBlock" HorizontalAlignment="Right" Foreground="#B7F2C5" Text="服务状态：未检测" />
                    <TextBlock x:Name="ApiBaseUrlTextBlock" Margin="0,6,0,0" HorizontalAlignment="Right" Foreground="#C9D7E8" Text="API：" />
                    <Button Width="120" Height="34" Margin="0,10,0,0" Content="刷新全部" Click="RefreshAllButton_Click" />
                </StackPanel>
            </Grid>
        </Border>

        <TabControl Grid.Row="1" Margin="20" Background="Transparent">
            <TabItem Header="设备管理">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock x:Name="OverviewSummaryTextBlock" FontSize="16" FontWeight="SemiBold" Foreground="#1B2D42" Text="加载中..." />
                    <WrapPanel x:Name="WorkshopCardsPanel" Grid.Row="1" Margin="0,14,0,10" />

                    <DockPanel Grid.Row="2">
                        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button Width="96" Margin="0,0,8,0" Content="新增设备" Click="AddDeviceButton_Click" />
                            <Button Width="96" Margin="0,0,8,0" Content="编辑设备" Click="EditDeviceButton_Click" />
                            <Button Width="96" Margin="0,0,8,0" Content="删除设备" Click="DeleteDeviceButton_Click" />
                            <Button Width="96" Content="刷新设备" Click="RefreshOverviewButton_Click" />
                        </StackPanel>

                        <DataGrid x:Name="DevicesGrid" Margin="0,10,0,0" SelectionMode="Single">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="车间" Binding="{Binding WorkshopName}" Width="120" />
                                <DataGridTextColumn Header="设备编码" Binding="{Binding DeviceCode}" Width="130" />
                                <DataGridTextColumn Header="设备名称" Binding="{Binding DeviceName}" Width="160" />
                                <DataGridTextColumn Header="控制器" Binding="{Binding ControllerModel}" Width="180" />
                                <DataGridTextColumn Header="IP" Binding="{Binding IpAddress}" Width="120" />
                                <DataGridTextColumn Header="端口" Binding="{Binding Port}" Width="70" />
                                <DataGridTextColumn Header="Agent 节点" Binding="{Binding AgentNodeName}" Width="130" />
                                <DataGridTextColumn Header="在线" Binding="{Binding MachineOnlineText}" Width="70" />
                                <DataGridTextColumn Header="当前状态" Binding="{Binding StateText}" Width="100" />
                                <DataGridTextColumn Header="健康度" Binding="{Binding HealthText}" Width="90" />
                                <DataGridTextColumn Header="当前程序号" Binding="{Binding CurrentProgramNo}" Width="110" />
                                <DataGridTextColumn Header="主轴转速" Binding="{Binding SpindleSpeedText}" Width="90" />
                                <DataGridTextColumn Header="主轴负载" Binding="{Binding SpindleLoadText}" Width="90" />
                                <DataGridTextColumn Header="采集质量" Binding="{Binding DataQualityCode}" Width="110" />
                                <DataGridTextColumn Header="最近采集" Binding="{Binding LastCollectedAtText}" Width="160" />
                                <DataGridTextColumn Header="最近心跳" Binding="{Binding LastHeartbeatAtText}" Width="160" />
                                <DataGridTextColumn Header="采集错误" Binding="{Binding LastCollectionError}" Width="180" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </DockPanel>
                </Grid>
            </TabItem>

            <TabItem Header="统计报表">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <DockPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock VerticalAlignment="Center" Text="统计日期：" />
                            <DatePicker x:Name="ReportDatePicker" Width="140" Margin="8,0,0,0" />
                            <Button Width="110" Margin="12,0,0,0" Content="刷新报表" Click="RefreshReportButton_Click" />
                        </StackPanel>
                        <TextBlock x:Name="ReportSummaryTextBlock" DockPanel.Dock="Right" VerticalAlignment="Center" FontWeight="SemiBold" Foreground="#1B2D42" />
                    </DockPanel>

                    <Grid Grid.Row="1" Margin="0,14,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <GroupBox Header="开机率公式" Margin="0,0,8,0">
                            <Grid Margin="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <TextBox x:Name="PowerOnFormulaTextBox" Height="56" TextWrapping="Wrap" AcceptsReturn="True" />
                                <Button Grid.Row="1" Width="120" Margin="0,10,0,0" HorizontalAlignment="Right" Content="保存开机率公式" Click="SavePowerOnFormulaButton_Click" />
                            </Grid>
                        </GroupBox>

                        <GroupBox Grid.Column="1" Header="利用率公式" Margin="8,0,0,0">
                            <Grid Margin="10">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <TextBox x:Name="UtilizationFormulaTextBox" Height="56" TextWrapping="Wrap" AcceptsReturn="True" />
                                <Button Grid.Row="1" Width="120" Margin="0,10,0,0" HorizontalAlignment="Right" Content="保存利用率公式" Click="SaveUtilizationFormulaButton_Click" />
                            </Grid>
                        </GroupBox>
                    </Grid>

                    <DataGrid x:Name="DailyReportGrid" Grid.Row="2" Margin="0,12,0,0">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="车间" Binding="{Binding WorkshopName}" Width="110" />
                            <DataGridTextColumn Header="设备编码" Binding="{Binding DeviceCode}" Width="120" />
                            <DataGridTextColumn Header="设备名称" Binding="{Binding DeviceName}" Width="150" />
                            <DataGridTextColumn Header="当前状态" Binding="{Binding CurrentStateText}" Width="90" />
                            <DataGridTextColumn Header="开机时间(分钟)" Binding="{Binding PowerOnMinutesText}" Width="110" />
                            <DataGridTextColumn Header="加工时间(分钟)" Binding="{Binding ProcessingMinutesText}" Width="110" />
                            <DataGridTextColumn Header="等待时间(分钟)" Binding="{Binding WaitingMinutesText}" Width="110" />
                            <DataGridTextColumn Header="待机时间(分钟)" Binding="{Binding StandbyMinutesText}" Width="110" />
                            <DataGridTextColumn Header="关机时间(分钟)" Binding="{Binding PowerOffMinutesText}" Width="110" />
                            <DataGridTextColumn Header="开机率" Binding="{Binding PowerOnRateText}" Width="90" />
                            <DataGridTextColumn Header="利用率" Binding="{Binding UtilizationRateText}" Width="90" />
                            <DataGridTextColumn Header="质量标记" Binding="{Binding DataQualityCode}" Width="110" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="设备运行情况">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal">
                        <TextBlock VerticalAlignment="Center" Text="设备：" />
                        <ComboBox x:Name="TimelineDeviceComboBox"
                                  Width="260"
                                  Margin="8,0,0,0"
                                  DisplayMemberPath="DisplayText"
                                  SelectedValuePath="DeviceId" />
                        <TextBlock Margin="20,0,0,0" VerticalAlignment="Center" Text="日期：" />
                        <DatePicker x:Name="TimelineDatePicker" Width="140" Margin="8,0,0,0" />
                        <Button Width="110" Margin="12,0,0,0" Content="刷新时间线" Click="RefreshTimelineButton_Click" />
                    </StackPanel>

                    <WrapPanel x:Name="TimelineTotalsPanel" Grid.Row="1" Margin="0,14,0,10" />

                    <DataGrid x:Name="TimelineGrid" Grid.Row="2">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="状态" Binding="{Binding StateText}" Width="120" />
                            <DataGridTextColumn Header="开始时间" Binding="{Binding StartAtText}" Width="180" />
                            <DataGridTextColumn Header="截止时间" Binding="{Binding EndAtText}" Width="180" />
                            <DataGridTextColumn Header="时长(分钟)" Binding="{Binding DurationMinutesText}" Width="110" />
                            <DataGridTextColumn Header="质量标记" Binding="{Binding DataQualityCode}" Width="140" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>

            <TabItem Header="用户与角色">
                <Grid Margin="12">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock FontSize="16" FontWeight="SemiBold" Foreground="#1B2D42" Text="用户、角色与权限矩阵" />

                    <Grid Grid.Row="1" Margin="0,12,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.2*" />
                            <ColumnDefinition Width="1*" />
                            <ColumnDefinition Width="1*" />
                        </Grid.ColumnDefinitions>

                        <GroupBox Header="用户">
                            <DataGrid x:Name="UsersGrid">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="用户名" Binding="{Binding UserName}" Width="120" />
                                    <DataGridTextColumn Header="显示名" Binding="{Binding DisplayName}" Width="110" />
                                    <DataGridTextColumn Header="部门" Binding="{Binding Department}" Width="110" />
                                    <DataGridTextColumn Header="角色" Binding="{Binding RoleCodesText}" Width="130" />
                                    <DataGridTextColumn Header="状态" Binding="{Binding EnabledText}" Width="80" />
                                    <DataGridTextColumn Header="最近登录" Binding="{Binding LastLoginAtText}" Width="150" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </GroupBox>

                        <GroupBox Grid.Column="1" Header="角色" Margin="12,0,12,0">
                            <DataGrid x:Name="RolesGrid">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="角色编码" Binding="{Binding RoleCode}" Width="100" />
                                    <DataGridTextColumn Header="角色名称" Binding="{Binding RoleName}" Width="100" />
                                    <DataGridTextColumn Header="说明" Binding="{Binding Description}" Width="160" />
                                    <DataGridTextColumn Header="权限数量" Binding="{Binding PermissionCount}" Width="80" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </GroupBox>

                        <GroupBox Grid.Column="2" Header="权限">
                            <DataGrid x:Name="PermissionsGrid">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="权限编码" Binding="{Binding PermissionCode}" Width="120" />
                                    <DataGridTextColumn Header="权限名称" Binding="{Binding PermissionName}" Width="100" />
                                    <DataGridTextColumn Header="说明" Binding="{Binding Description}" Width="170" />
                                </DataGrid.Columns>
                            </DataGrid>
                        </GroupBox>
                    </Grid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
